console.log(
  [
    "DocGraph V4 monorepo skeleton is ready.",
    "Install workspace dependencies before running the desktop or sidecar dev servers.",
    "Suggested setup: corepack enable; corepack prepare pnpm@9.15.4 --activate; pnpm install",
  ].join("\n"),
);

