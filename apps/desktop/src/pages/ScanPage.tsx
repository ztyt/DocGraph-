import { PlaceholderPage } from "../components/system/PlaceholderPage";

export function ScanPage() {
  return (
    <PlaceholderPage
      eyebrow="Scan"
      title="Scan center"
      successTitle="Scan route ready"
      successBody="Folder selection, progress, pause, and retry controls start in the scan milestone."
      emptyTitle="No scan jobs"
      emptyBody="No folder scan has been created in this workspace."
    />
  );
}

